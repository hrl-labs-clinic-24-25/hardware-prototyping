void setup() {
  // put your setup code here, to run once:
  const int SCK = 13;
  const int MISO = 12;
  const int MOSI = 11;
  const int CS = 10;
  pinMode (SCK, INPUT);
  pinMode (MISO, INPUT);
  pinMode (MOSI, INPUT);
  pinMode (CS, INPUT);
}

void loop() {
  // put your main code here, to run repeatedly:
  while (1) {}
}
